#ifndef AVRUTILS_PIN_MODE_HPP
#define AVRUTILS_PIN_MODE_HPP

namespace avrutils {
namespace io {

enum PinMode {
    INPUT = false,
    OUTPUT = true,
};

enum PinValue {
    LOW = false,
    HIGH = true,
};


};
};

#endif